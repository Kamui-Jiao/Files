import matplotlib.pyplot as plt
import numpy as np
import math
z = 500
x = np.array([x for x in range(z)])
lst = []
for i in range(z):
    lst += [foo(i)]
y = np.array(lst)
plt.plot(x,y)
plt.plot(np.array([x for x in range(z)]),np.array([2*math.log(x) for x in range(1,z+1)]))
plt.show()
